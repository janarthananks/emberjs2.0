import { randomImagePosition } from '../../../helpers/random-image-position';
import { module, test } from 'qunit';

module('Unit | Helper | random image position');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = randomImagePosition(42);
  assert.ok(result);
});

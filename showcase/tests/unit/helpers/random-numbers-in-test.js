import { randomNumbersIn } from '../../../helpers/random-numbers-in';
import { module, test } from 'qunit';

module('Unit | Helper | random numbers in');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = randomNumbersIn(42);
  assert.ok(result);
});
